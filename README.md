Enter the path to the folder of images in input_folder in main.py and then run it to get the models predictions.

Trained on 40X histological images, with an average accuracy of 89% on unseen images.
